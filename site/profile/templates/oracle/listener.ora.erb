# listener.ora Network Configuration File: /opt/oracle/product/12.1.0/dbhome_1/network/admin/listener.ora
# Generated by Oracle configuration tools.

<%= @listener.upcase %> =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST =<%= @listener_host %>) (PORT =<%= @db_port %>))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC12000))
    )
  )

SID_LIST_<%= @listener %>=
(SID_LIST=
 (SID_DESC=
  (SID_NAME=<%= @db_name %>)
  (ORACLE_HOME=<%= @oracle_home %>)))
